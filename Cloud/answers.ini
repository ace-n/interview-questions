[cloud_networking]
1A=prioritize throughput while maintaining acceptable latency
1B=throughput > latency
2A=yes
3A=convential proxies = clients; reverse proxies = servers
3B=convential proxy = clients; reverse proxy = servers
3C=convential = clients; reverse = servers
4A=load balancer = routing; reverse proxy = routing + modification
4B=reverse proxies can modify requests; load balancers can't
5A=active-active, active-passive
6A=keep some machines in reserve that are only used if errors occur
7A=traffic splitting
8A=yes
9A=push, pull
9B=pull, push
10A=CDN caches point-in-time "snapshots" of content via timestamped/versioned URLs
11A=CDN "pulls" new versions of content down periodically
12A=random, round robin, least busy
13A=software load balancing
14A=load balancer decrypts incoming requests; decryption can be expensive
15A=yes
16A=packet metadata (IP addresses, ports, etc)
17A=packet's application-level data
17B=packet contents
18A=no,yes
19A=speed;flexibility
19B=faster;more flexible
20A=map external (global) IP addresses to internal (local) ones
21A=one external IP address maps to multiple internal ones
21B=one global IP address maps to multiple local ones
22A=map incoming packets' sources to active connections' destinations

[cloud_databases]
1A=return errors if your queue is overloaded
2A=databases can only have two of {consistency, availability, partition tolerance}
3A=yes; network issues may cause partitions (between writes)
4A=weakly consistent operations may never complete, while eventually consistent ones will
4B=weakly consistent = may not complete
5A=atomicity,consistency,isolation,durability
6A=leader handles ALL writes; followers ONLY handle reads
7A=multiple leaders coordinate with each other on writes
8A=yes
9A=each model/object has its own DB instance
10A=yes; may slow down DB operations
11A=distributing a single model/object across multiple DBs
12A=yes; one shard may contain too many (or too few) records
12B=yes; one shard may contain too many records
13A=basically available, soft state (eventual consistency)
14A=hash tables
15A=redis, memcached
16A=document databases are built on top of KV stores
17A=rows are tuples of TABLES, not (primitive) values
18A=wide-column database
18B=wide-column datastore
18C=wide-column
19A=graph database
19B=graph
20A=DB queries with results, individual objects
21A=application-level
22A=cache-aside, write-through, write-back, refresh-ahead
23A=automatically update any cache entries based on a TTL
24A=IGNORE
25A=properties that a database transaction should have
26A=IGNORE
26B=IGNORE

[clean_architecture]
1A=no
2A=no
3A=individual actions within overall business logic
4A=no;no
5A=only business-logic-related ones
6A=convert data to/from format used by entities
7A=external interfaces
8A=no

[osi_model]
1A=physical; cables
2A=data-link; frames
3A=network; packets
4A=transport; protocols
5A=session; open persistent connections between ports
6A=presentation; file encodings/formats
6B=presentation; file formats
7A=application; application data
8A=frames wrap packets with an (additional) MAC address
8B=frames add MAC addresses to packets
9A=Network layer (#3)
9B=Network
10A=Please do not throw sausage pizza away
10B=All people seem to need data processing
11A=REST uses URL {parameters, hierarchy}, RPC uses specific method names + HTTP method parameters
12A=/users/list
13A=/listUsernames

[leetcode]
1A=XOR all the numbers together
1B=XOR them
2A=dynamic programming computes the entire state space; memoization only handles repeats
3A=bottom-up
4A=top-down

[architecture_patterns]
1A=helper services send requests on behalf of someone else; common settings can be abstracted away
2A=proxy requests between a legacy and a modern system
3A=create separate backends for each frontend (to prefer customizability over reuse)
4A=load-balance requests between a legacy and a modern system, and gradually move requests over to the modern system
5A=command-query responsibility segregation; use separate data stores for reads and writes
6A=allow others to modify data while rejecting conflicting writes
7A=a transaction-like set of actions that uses retry logic instead of locking
7B=a set of actions that are executed (or rolled back) as a unit, without any kind of locking in between
8A=Standard transactions are ACID compliant, sagas are not
9A=compensable, pivot, retryable
10A=a transaction that can be reversed
11A=if the pivot transaction is completed, the remaining transactions in the saga must also be completed
12A=a transaction that follows the pivot transaction **AND** is guaranteed to succeed

[domain_driven_design]
1A=Designing software so that it matches the business' needs
2A=A group of Entities that are both necessary and sufficient for a particular set of business logic
3A=Domain events are specific to the business domain, not generic. (e.g. "pizza delivered" vs. "DB updated")
4A=domain services, application services
4B=domain, application
5A=a service that encapsulates domain-specific logic
6A=a service that isn't specific to the domain (e.g. an SMS notification service)

[transactions_concurrency]
1A=manual intervention
2A=no read locks, lock on atomic reads, lock current row UTE, lock all definitely-affected rows UTE, lock all potentially-affected rows UTE (until transaction end)
3A=non-locking DBs abort a "locking" transaction, while locking DBs wait for the lock
4A=DBs keep records ("snapshots") of all their values at different times
5A=yes, yes
6A=linearizability
7A=2-phase locking; all relevant rows are locked prior to reading (phase 1) and unlocked after writing (phase 2)
8A=objects that get added to/removed from a DB in between transaction start and end; can cause race conditions
9A=pessimistic approaches wait for locks, while optimistic approaches fail and demand a(n external) retry
10A=no race conditions within individual objects
11A=yes
12A=linearizability; avoids race conditions when enforcing uniqueness constraints
13A=always (even during normal system operation!)
14A=everything is in order (linearizability) vs. some things are in order (causality)
15A=faster when networks slow down, available when networks fail
16A=add a target version number to the write operation
17A=ask all nodes if they can perform a transaction; and then have them perform it
18A=yes - the coordinating node could fail in between phases

[replication_consensus]
1A=distributed uuid generation
2A=split writes to a value across multiple keys and aggregate their results together; enables concurrent write operations to the same underlying key
3A=a large constant; DB node count; DB entry count
4A=5 ms; 100 ms
5A=physical clocks measure the passage of time; logical clocks measure event counts
6A=order operations by a (logical timestamp, node ID) tuple
7A=when all nodes receive messages in identical order; linearizable writes
8A=Spanner delays actions until the uncertainty period has elapsed
9A=it can tolerate any possible fault from a limited number of nodes
10A=slower writes
11A=eventually-consistent reads
12A=promote the most up-to-date follower to leader status
13A=when two nodes think they should both be the leader
14A=quorums; kingmaker/tiebreaker nodes; fencing tokens
15A=nodes randomly vote for a leader until a clear majority is reached
16A=leaders are given a strictly-increasing "generation" value; any previous-generation leaders are ignored
17A=token generators must not contract split-brain syndrome themselves (i.e. they must be globally consistent)
18A=read potentially-modified data directly from the leader node; reads from other nodes may return outdated data
19A=offline NoSQL storage
20A=last write wins; break ties randomly; merge records together; resolve conflicts at the application layer
21A=when replicating a DB across multiple datacenters; each datacenter should have a single leader node
22A=the path(s) that data-replication instructions take through a DB system's network
23A=all-to-all, circular, star (one-to-all)
24A=sending write instructions to every DB instance
25A=quorum-based reads/writes
26A=single-replica read requests could hit an outdated replica
27A=multi-leader systems with each datacenter having its own leader
28A=partitioning them across machines based on the value to be checked
29A=rerun the election process until a winner emerges
30A=uniform agreement (all nodes agree on the same value), integrity (nodes agree exactly once), validity (nothing not proposed can be agreed to), termination (an agreement will happen eventually)
31A=2PC requires unanimous approval, consensus requires a majority
32A=distributed key-value stores
33A=linearizable, total ordering, failure detection, change notifications
34A=fixed (constant)
35A=no; writes aren't fast enough
36A=configuration storage; service discovery
37A=services should be able to route to other services without needing to know their specific IPs
38A=cloud-native systems create and destroy machines at will, which causes frequent IP changes

[architecture_messaging_resilience]
1A=helper services send requests on behalf of someone else; common settings can be abstracted away
2A=proxy requests between a legacy and a modern system
3A=create separate backends for each frontend (to prefer customizability over reuse)
4A=load-balance requests between a legacy and a modern system, and gradually move requests over to the modern system
5A=Designing software so that it matches the business' needs
6A=A group of Entities that are both necessary and sufficient for a particular set of business logic
7A=Domain events are specific to the business domain, not generic. (e.g. "pizza delivered" vs. "DB updated")
8A=domain, application
9A=a service that encapsulates domain-specific logic
10A=a service that isn't specific to the domain (e.g. an SMS notification service)
11A=we should be able to reconstruct a system exact behavior's from logs and metrics alone
12A=limit request volume to unhealthy services, which buys those services time to recover
13A=faults are when parts of a system break; failures are when the entire system breaks
14A=99.9 (three nines); latency values higher than this are usually noisy
15A=the messaging service (NOT the web application)
16A=no locks; reduced cross-service ("cascading") failure risk
17A=fully encapsulated (no cross-service state), communicate entirely via async message-passing
18A=shuttle messages between services
19A=send to everyone (fan-out); send to random consumer (load balancing)
20A=not if they have multiple consumers; use one broker per consumer
21A=extracting the change-sequence from one system and propagating them to another
22A=subscribing to conversion rate updates; conversion rate service is no longer a SPOF (single point of failure)
23A=read-stage; reduces coupling between the storage technologies

[storage_indexing_analytics]
1A=NoSQL databases are a better fit for application code, and thus have a lower "impedance mismatch" than SQL options
2A=make multiple queries at the application layer
3A=breaking down singular application-level objects into multiple separate tables
4A=DB-wide queries are harder to parallelize / optimize for?
5A=side effects make (automated) optimization more difficult
6A=databases that store edges of a graph as (subject, predicate, object) tuples
7A=edge list
8A=an append-only file (AOF) that lists all DB transactions
9A=a portion of a DB's log; different segments can be stored on separate disks (which avoids disk overruns)
10A=they can be used to reconstruct the DB from scratch
11A=compaction; throwing away changes that have since been overwritten
12A=adding indexes slows down writes, so they must be used sparingly
13A=sorted string table; key-value store where entries are sorted in key-order
14A=they simplify the segment-merging and indexing processes
15A=A storage system that uses SSTrees (trees of SSTables) during segment/log merging
16A=no
17A=split it into multiple sub/child-pages
18A=yes!
19A=yes; DB faults during a multi-page write can causes corrupted indexes
20A=Store a log of uncommitted writes (the "write-ahead log")
21A=LSM-trees (for writes); B-trees (for reads)
22A=primary indexes are based on primary keys; secondary indexes are based on other columns
23A=individual word(s) within the corpus
24A=convert documents into a hash (binary, n-gram based, etc.) and search among a hash set
25A=convert documents into a vector using sentence embeddings, and search among a vector set
26A=yes; they're significantly faster (but more expensive) than disk-based DBs
27A=a read-only copy of a DB used purely for analytics purposes
28A=analytics
29A=slower to write to; faster for typical analytics queries
30A=cached queries
31A=cached copies of *derived* data-tables; they're DERIVED and cannot be modified
32A=those whose records can be broken up into categories along multiple axes (one axis per attribute)
33A=each OLAP categorization axis can be modeled as an axis of a hypercube
34A=N
35A=migrate values to the new schema explicitly (as in SQL DBs); handle schema deviations at the application layer
36A=a platform for running MapReduce jobs (typically as JARs) across server clusters
37A=Hadoop Distributed File System
38A=yes
39A=map, shuffle/sort, reduce
40A=generate values for a set of keys
41A=sort keys within a partition; makes aggregating ("reducing") keys easier when result-sets don't fit in RAM
42A=take sorted values (generated across multiple machines) and aggregate them together
43A=the key-space (NOT the original dataset itself!)
44A=fault tolerance on unreliable machines
45A=large-scale data analysis pipelines
46A=avoids writing to disk between steps; supports more generalized workflows
47A=SQL table
48A=single [beefy] machine

[serialization_security_rpc]
1A=when they can instantiate arbitrary classes with arbitrary values
2A=33% more
3A=protobuf
4A=protobuf, but dynamically-typed
5A=abstract away HTTP method calls as ordinary methods; HTTP failures can break this abstraction

[cap_theorem]
1A=linearizability
2A=any operations that START after a previous operation FINISHES will see the effects of the previous operation
3A=no; partitions refer to communication/network failures - not latency
4A=yes; partitions refer to network failures - so partition tolerance is unnecessary if these failures can't happen
5A=no
6A=transactions cannot partially succeed - they must be 100% failures or 100% successes
7A=transactions cannot corrupt DB-level constraints (such as foreign keys)
8A=DB operations can be parallelized without corrupting one another
9A=completed transactions must be recoverable even if the database crashes
10A=atomicity, consistency, isolation, durability
11A=basically available, soft state, eventual consistency
12A=ACID prioritizes consistency; BASE prioritizes availability
13A=SQL; NoSQL (e.g. MongoDB)

[caching_and_redis]
1A=short-lived cache entries w/ server revalidation, "eternal" cache entries w/ changing URLs
2A=not many repeated queries, cache is too small to fit repeated queries
3A=A cache that is on the same machine as the data being served
4A=cached data may be inconsistent across servers, cache misses are O(machine count), caches take time to warm up ("cold starts")
5A=removing outdated values (expiration) vs. unused ones (eviction)
6A=a soft TTL indicates when to try fetching a new value, while a hard TTL indicates when to evict a current value
7A=yes; failure to cache errors may DoS the downstream service
8A=request coalescing
9A=Ensuring only a single request is sent to the downstream service at a time
10A=the Least Frequently Used (LFU) eviction policy
11A=an in-memory key-value store; caching
12A=yes!
13A=append-only files (AOFs), database dumps (RDB files)
14A=AOFs are larger, but more resistant to outages / corruption
15A=consistent hashing, virtual nodes
16A=evenly distribute incoming data across machines, most hashes don't change much when the number of machines changes
17A=RAID for caches; distribute objects evenly across machines of varying size/performance
18A=scatter-gather
19A=cache-through operates within the database/storage layer; cache-aside writes happen within the application layer (our code)
20A=refreshes cache entries periodically before they expire

[nines_table]
1A=financial transactions; telecoms
2A=video / audio streaming
3A=e-commerce
4A=internal tools
5A=batch processing; ETL jobs
